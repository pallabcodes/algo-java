package recursion.backtrack;

public class Backtracking {
    // 1. when one answer from recursive call gives a wrong output & therefore all other (previous) answers becomes wrong; then backtrack
    // 2. whatever changes made during "recursive call"; just revert it when returning (i.e. popping out from call stack)
}
